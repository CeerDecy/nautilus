agent:
  engine: "${AGENT_ENGINE}"
  model: "${AGENT_MODEL}"
  token: "${AGENT_TOKEN}"
  baseUrl: "${AGENT_BASEURL}"

mq:
  host: "${MQ_HOST}"
  port: "${MQ_PORT}"
  topic: "${MQ_TOPIC}"
  server_type: "${MQ_SERVER_TYPE}"